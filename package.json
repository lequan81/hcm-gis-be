{
  "name": "hcm-tiles-backend",
  "version": "2.0.0",
  "description": "HCMC 3D Building Tiles — Bun API backend",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "build": "bun run build.ts",
    "start": "NODE_ENV=production bun run dist/main.js"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^2.0.3",
    "archiver": "^7.0.1",
    "pbf": "^4.0.1"
  },
  "devDependencies": {
    "@types/archiver": "^7.0.0",
    "@types/bun": "^1.3.10"
  }
}
